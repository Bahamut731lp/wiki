Ukazatel (anglicky **pointer**) je [[Abstraktní datový typ|abstraktní datový typ]], který uchovává adresu v operační paměti. Ukazatele mají svůj typ, který je totožný s datovým typem dat, která jsou uložena v paměti (`int` ukazatel ukazuje na data typu `int`, ...)